version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable

    steps:
      - checkout

      - run:
          name: Show Files
          command: ls -la

      - run:
          name: Run Build Script
          command: |
            chmod +x scripts/build.sh
            ./scripts/build.sh

workflows:
  version: 2

  build:
    jobs:
      - build
