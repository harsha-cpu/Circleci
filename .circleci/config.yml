version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable

    steps:
      - checkout
      - run:
          name: Test
          command: echo "CircleCI is working!"

workflows:
  version: 2
  build:
    jobs:
      - build
